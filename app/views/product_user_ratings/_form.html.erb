<%= form_for(@product_user_rating) do |f| %>
  <% if @product_user_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_user_rating.errors.count, "error") %> prohibited this product_user_rating from being saved:</h2>

      <ul>
      <% @product_user_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
